rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The current website uses Realtime Database for reviews.
    // Firestore is intentionally locked until a Firestore collection is explicitly added.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
